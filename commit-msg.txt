Make default assignee and time hours configurable via environment variables

- Add REDMINE_DEFAULT_ASSIGNEE_ID env var for default issue assignee
- Add REDMINE_DEFAULT_TIME_HOURS env var for default time logging
- Both are optional and won't affect behavior if not set
- Makes the MCP server publishable without hardcoded values
